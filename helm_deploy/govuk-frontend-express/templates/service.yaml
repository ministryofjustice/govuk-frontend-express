apiVersion: v1
kind: Service
metadata:
  name: {{ include "govuk-frontend-express.fullname" . }}
  labels:
    {{- include "govuk-frontend-express.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "govuk-frontend-express.selectorLabels" . | nindent 4 }}
